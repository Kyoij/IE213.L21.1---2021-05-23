<!DOCTYPE html>
<html lang="en">
    <%- include('components/head',{title}); -%>
    <body>
        <%- include('components/navbar',{nav:'detail',search:""}); -%>
        <div class="container">
            <main>
                <div class="py-5 text-center">
                    <h2>Thêm hoa vào từ điển</h2>
                </div>
                <form class="needs-validation" novalidate method="POST" action="/flower" enctype="multipart/form-data">
                    <div class="row g-5">
                        <div class="col-md-5 col-lg-4 order-md-last">
                            <h4 class="d-flex justify-content-between align-items-center mb-3">
                                <span class="text-primary">Hình ảnh</span>
                            </h4>
                            <div style="width: 100%; padding-top: 100%; position: relative; margin-bottom: 15px">
                                <img
                                    style="
                                        width: 100%;
                                        height: 100%;
                                        position: absolute;
                                        top: 0;
                                        left: 0;
                                        object-fit: cover;
                                    "
                                    id="imgsrc"
                                    src="/images/<%=flower.image%>"
                                />
                            </div>
                        </div>
                        <div class="col-md-7 col-lg-8">
                            <h4 class="mb-3">Thông tin chi tiết</h4>
                            <div class="row g-3">
                                <div class="col-12">
                                    <label for="name" class="form-label">Tên hoa</label>
                                    <div class="input-group has-validation">
                                        <input
                                            type="text"
                                            class="form-control"
                                            id="name"
                                            name="name"
                                            value="<%=flower.name%>"
                                            readonly
                                        />
                                    </div>
                                </div>
                                <div class="col-12">
                                    <label for="description">Giới thiệu</label>
                                    <div class="form-group input-group has-validation">
                                        <textarea
                                            class="form-control"
                                            id="description"
                                            name="description"
                                            rows="3"
                                            readonly
                                        >
<%=flower.description%></textarea
                                        >
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </form>
            </main>
        </div>
    </body>
</html>
